// every go files belong to a package
// identify the package that belongs to on the top of the file

package main // tells the compiler to look for entrey point of the function

import (
	"fmt"
)

func main() {
	fmt.Println("Hello world!")
}
